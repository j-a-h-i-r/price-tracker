doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        style.
            body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
            .container { max-width: 600px; margin: 0 auto; padding: 20px; }
            .product { margin-bottom: 20px; padding: 15px; border: 1px solid #ddd; border-radius: 4px; }
            .price { color: #2ecc71; font-weight: bold; }
            .target-price { color: #7f8c8d; }
            .cta { display: inline-block; padding: 10px 20px; background-color: #3498db; color: white; text-decoration: none; border-radius: 4px; }

    body
        .container
            h3 Price Alert: Products Below Target Price
            p We found some products that are currently below your target price:
            
            each product in products
                .product
                    h2= product.product_name
                    p
                        span.price Current Price: #{product.current_price}
                        br
                        span.target-price Target Price: $#{product.target_price}
                    a.cta(href=product.product_url) View Product

            p Happy shopping!